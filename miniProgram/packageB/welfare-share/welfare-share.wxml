<!--packageB/invitation-code-share/invitation-code-share.wxml-->
<view class="invitation-code-share">
    <view class="share-buttons">
        <button class="row" open-type="share" wx:for="{{list}}" wx:key="{{index}}" data-item="{{item}}">
            <view class="left">{{item.pickup_name}}</view>
        </button>
    </view>
</view>
